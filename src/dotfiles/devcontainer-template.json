{
  "id": "dotfiles",
  "version": "1.0.0",
  "name": "Dotfiles Dev Environment",
  "description": "A dev container with Fish shell, starship prompt, your dotfiles, and AWS SSO integration pre-configured for a personalized development experience.",
  "documentationURL": "https://github.com/technicalpickles/dotfiles-devcontainer",
  "options": {
    "dotfilesRepo": {
      "type": "string",
      "description": "Dotfiles repository URL",
      "default": "https://github.com/technicalpickles/dotfiles.git"
    },
    "dotfilesBranch": {
      "type": "string",
      "description": "Dotfiles repository branch",
      "default": "main"
    },
    "userShell": {
      "type": "string",
      "description": "Login shell path for the vscode user",
      "default": "/usr/bin/fish"
    },
    "userShellName": {
      "type": "string",
      "description": "VS Code terminal profile name for the shell",
      "default": "fish"
    },
    "installEnvVars": {
      "type": "object",
      "description": "Environment variables to set before running install.sh",
      "default": {},
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "platforms": ["Any"]
}
